ghostProtocol: https
ghostPath: /
ghostUsername: ${GHOST_ADMIN_USER}
ghostPassword: ${GHOST_ADMIN_PASSWORD}
ghostEmail: ${EMAIL}
ghostBlogTitle: Bare Metal Blog
allowEmptyPassword: false
smtpHost: ${SMTP_HOST}
smtpPort: ${SMTP_STARTTLS_PORT}
smtpUser: ${SMTP_USERNAME}
smtpPassword: ${SMTP_PASSWORD}
smtpFromAddress: ${SMTP_FROM}
smtpService: 
externalDatabase:
  host: localhost
  user: ${GHOST_MARIADB_USER}
  password: ${GHOST_MARIADB_PASSWORD}
  database: ${GHOST_MARIADB_USER}
  port: 3306
mariadb:
  enabled: true
  architecture: standalone
  auth:
    rootPassword: ${GHOST_MARIADB_ROOT_PASSWORD}
    database: ${GHOST_MARIADB_USER}
    username: ${GHOST_MARIADB_USER}
    password: ${GHOST_MARIADB_PASSWORD}
  primary:
    persistence:
      enabled: true
      storageClass: longhorn-weekly
      accessModes:
      - ReadWriteOnce
      size: 8Gi
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: internal
  hosts:
  - name: blog.${INTERNAL_DOMAIN}
    path: /
    tls: false