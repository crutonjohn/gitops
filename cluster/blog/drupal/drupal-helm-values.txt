drupalUsername: ${DRUPAL_ADMIN_USER}
drupalPassword: ${DRUPAL_ADMIN_PASSWORD}
drupalEmail: ${EMAIL}
ingress:
    enabled: true
    certManager: false
    hostname: drupal.${INTERNAL_DOMAIN}
    annotations:
      kubernetes.io/ingress.class: internal
    hosts:
    - name: drupal.${INTERNAL_DOMAIN}
      path: /
## SMTP mail delivery configuration
## ref: https://github.com/bitnami/bitnami-docker-drupal/#smtp-configuration
##
smtpHost: ${SMTP_HOST}
smtpPort: ${SMTP_STARTTLS_PORT}
smtpUser: ${SMTP_USERNAME}
smtpPassword: ${SMTP_PASSWORD}
smtpProtocol: tls
mariadb:
  fullnameOverride: maria-drupal
  auth:
    username: ${DRUPAL_MARIADB_USER}
    password: ${DRUPAL_MARIADB_PASSWORD}
    rootPassword: ${DRUPAL_MARIADB_ROOT_PASSWORD}
    database: drupal
externalDatabase:
  host: maria-drupal.blog.svc.cluster.local
  port: 3306
  user: ${DRUPAL_MARIADB_USER}
  password: ${DRUPAL_MARIADB_PASSWORD}
  database: drupal