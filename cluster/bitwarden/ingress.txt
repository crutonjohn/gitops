---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/target: ${EXTERNAL_IP}
    kubernetes.io/ingress.class: "external"
  name: bitwarden
  namespace: bitwarden
spec:
  rules:
  - host: bitwarden.${INTERNAL_DOMAIN}
    http:
      paths:
      - backend:
          serviceName: bitwarden
          servicePort: 80
        path: /
  tls:
  - hosts:
    - bitwarden.${INTERNAL_DOMAIN}
    secretName: bitwarden-crutonjohn-com