apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud
  namespace: nextcloud
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - match: Host(`cloud.${EXTERNAL_DOMAIN}`)
    kind: Rule
    services:
    - name: nextcloud-nginx
      port: 80
    middlewares:
    - name: default-http-redirect@kubernetescrd
  tls:
    store:
      name: default