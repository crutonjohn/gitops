---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-config
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  volumeName: home-assistant-nvmeof
  storageClassName: truenas-manual
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: home-assistant-nvmeof
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: truenas-manual
  mountOptions: []
  csi:
    driver: org.democratic-csi.node-manual
    readOnly: false
    fsType: ext4
    volumeHandle: home-assistant-nvmeof
    volumeAttributes:
      transport: tcp://192.168.130.49:4420
      # transports: <transport>,<transport>,...
      nqn: homeassistant
      nsid: "1"
      node_attach_driver: "nvmeof"
      provisioner_driver: node-manual
