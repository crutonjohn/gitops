---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: paperless-config
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: codex-block
