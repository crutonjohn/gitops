---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: acore-db-data
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: codex-block
  resources:
    requests:
      storage: 30Gi
