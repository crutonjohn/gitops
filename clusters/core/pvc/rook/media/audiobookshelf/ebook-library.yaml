---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebook-library
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: terra-filesystem
  resources:
    requests:
      storage: 50Gi
