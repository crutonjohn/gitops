---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music-library
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1024Gi
  storageClassName: ceph-filesystem
