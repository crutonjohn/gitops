---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn
  annotations:
    kubernetes.io/ingress.class: "traefik-internal"
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`longhorn.${FAMILY_DOMAIN}`)"
      middlewares:
        - name: authentik
          namespace: auth-system
      services:
        - name: longhorn-frontend
          port: 80
  tls:
    secretName: heyjohn-wildcard-certificate
    domains:
      - main: longhorn.${FAMILY_DOMAIN}
# ---
# apiVersion: externaldns.k8s.io/v1alpha1
# kind: DNSEndpoint
# metadata:
#   name: navidrome
#   annotations:
#     external-dns/public: "true"
# spec:
#   endpoints:
#     - dnsName: music.${FAMILY_DOMAIN}
#       recordTTL: 300
#       recordType: CNAME
#       targets:
#         - ${DYNAMIC_DOMAIN}
