---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: linkerd-multicluster
spec:
  values:
    gateway:
      serviceType: LoadBalancer
      loadBalancerIP: ${CLUSTER_LB_LINKERD}
