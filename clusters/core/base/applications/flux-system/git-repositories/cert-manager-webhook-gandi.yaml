---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cert-manager-webhook-gandi
  namespace: flux-system
spec:
  ignore: |
    # exclude all
    /*
    # path to chart
    !/deploy/cert-manager-webhook-gandi
  interval: 1h
  ref:
    tag: v0.2.4
  timeout: 60s
  url: https://github.com/molnett/cert-manager-webhook-gandi.git
