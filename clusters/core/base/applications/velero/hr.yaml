---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: velero
spec:
  interval: 5m
  chart:
    spec:
      chart: velero
      version: 2.28.1
      sourceRef:
        kind: HelmRepository
        name: tanzu-charts
        namespace: flux-system
  values:
    image:
      repository: velero/velero
      tag: "v1.8.1"
