---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: submariner-broker
spec:
  interval: 5m
  chart:
    spec:
      chart: submariner-k8s-broker
      version: 0.12.0
      sourceRef:
        kind: HelmRepository
        name: submariner-charts
        namespace: flux-system
  values:
    crd:
      create: false
