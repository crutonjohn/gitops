---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - hr.yaml
  - env.enc.yaml
configMapGenerator:
  - name: litellm-config
    files:
      - config.yaml=./config.yaml
generatorOptions:
  disableNameSuffixHash: true
