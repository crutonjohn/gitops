---
kind: ServiceAccount
apiVersion: v1
metadata:
  name: mediamtx-rclone
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: mediamtx-rclone
spec:
  concurrencyPolicy: Forbid
  schedule: "45 5/12 * * *" # Twice a day at 5:45 && 17:45
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 10
  startingDeadlineSeconds: 60
  timeZone: "${LOCAL_TIMEZONE}"
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 12000
      template:
        spec:
          serviceAccountName: mediamtx-rclone
          restartPolicy: Never
          containers:
            - name: rclone
              image: rclone/rclone:latest@sha256:8a17d9b5cd5ce71bbb42e49e92ee83575d7fb03f6233d949d328e9d029b9376d
              command: ["rclone"]
              args:
                - "move"
                # - "--transfers=2"
                - "-v"
                - "--config"
                - "/rclone.conf"
                # - "--ca-cert"
                # - "/ca.crt"
                # - "--include"
                # - "*.mp4"
                - "/recordings/"
                - "perturabo-smb-video-talos:/videos/games/"
              volumeMounts:
                - name: mediamtx-rclone-config
                  mountPath: /rclone.conf
                  subPath: rclone.conf
                  readOnly: true
                - name: mediamtx-recordings
                  mountPath: /recordings
                - name: heyjohn-internal-ca
                  mountPath: /ca.crt
                  subPath: ca.crt
                  readOnly: true
          volumes:
            - name: heyjohn-internal-ca
              secret:
                secretName: heyjohn-internal-ca
            - name: mediamtx-rclone-config
              secret:
                secretName: mediamtx-rclone-config
            - name: mediamtx-recordings
              persistentVolumeClaim:
                claimName: mediamtx-recordings
