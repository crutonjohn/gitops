---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - hr.yaml
  - slskd.enc.yaml
configMapGenerator:
  - name: slskd-config
    files:
      - slskd.yml=./config.yaml
generatorOptions:
  disableNameSuffixHash: true
