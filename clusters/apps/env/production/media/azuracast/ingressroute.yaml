---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: azuracast
  annotations:
    kubernetes.io/ingress.class: "traefik-external"
    external-dns.alpha.kubernetes.io/hostname: radio.${FAMILY_DOMAIN}
    external-dns.alpha.kubernetes.io/target: ${DYNAMIC_DOMAIN}
    external-dns/public: "true"
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`radio.${FAMILY_DOMAIN}`)"
      services:
        - name: azuracast-navidrome
          port: 80
  tls:
    secretName: heyjohn-wildcard-certificate
    domains:
      - main: radio.${FAMILY_DOMAIN}
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: azuracast
  annotations:
    external-dns/public: "true"
spec:
  endpoints:
    - dnsName: radio.${FAMILY_DOMAIN}
      recordTTL: 300
      recordType: CNAME
      targets:
        - ${DYNAMIC_DOMAIN}
