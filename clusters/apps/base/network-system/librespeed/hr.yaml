metadata:
  name: librespeed
  namespace: network-system
spec:
  interval: 5m
  chart:
    spec:
      chart: librespeed
      version: 2.0.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: flux-system
