---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: keycloak
spec:
  interval: 5m
  chart:
    spec:
      chart: keycloak
      version: 7.1.8
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system
  dependsOn:
    - name: keycloak-db
  values:
    image:
      registry: docker.io
      repository: bitnami/keycloak
      tag: 16.1.1-debian-10-r66
