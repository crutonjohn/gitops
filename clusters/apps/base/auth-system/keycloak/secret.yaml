---
apiVersion: v1
stringData:
  admin-password: ${KEYCLOAK_ADMIN_PASSWORD}
  management-password: ${KEYCLOAK_MANAGEMENT_PASSWORD}
  database-password: ${KEYCLOAK_DATABASE_PASSWORD}
  postgresql-password: ${KEYCLOAK_DATABASE_PASSWORD}
kind: Secret
metadata:
  name: keycloak-secrets
  namespace: auth-system
type: Opaque
