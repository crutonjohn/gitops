apiVersion: v1
kind: Secret
metadata:
  name: "${SRC_PVC}-b2-restic"
type: Opaque
stringData:
  RESTIC_REPOSITORY: ${VOLSYNC_B2_S3}/restic/${SRC_PVC}
  RESTIC_PASSWORD: ${VOLSYNC_RESTIC_PASSWORD}
  AWS_ACCESS_KEY_ID: ${VOLSYNC_B2_AWS_ACCESS_KEY_ID}
  AWS_SECRET_ACCESS_KEY: ${VOLSYNC_B2_AWS_SECRET_ACCESS_KEY}
