apiVersion: v1
kind: Secret
metadata:
    namespace: media
    name: music-sshfs-secret
type: Opaque
stringData:
    ROOT_PASSWORD: ${MUSIC_SSHFS_ROOT_PASSWORD}
    ROOT_AUTHORIZED_KEY: ${MUSIC_SSHFS_AUTHORIZED_KEY}
